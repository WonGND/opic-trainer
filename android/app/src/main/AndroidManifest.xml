<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 로컬 알림: Android 13(API 33)+ 는 런타임 권한이 필수입니다.
         매니페스트 선언만으로는 알림이 뜨지 않고, 앱에서 requestPermissions() 를 호출해야 합니다. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 정확한 시각 알림. SCHEDULE_EXACT_ALARM 은 Android 13, USE_EXACT_ALARM 은 14+ 용입니다.
         절전 모드에서도 정시에 울리도록 allowWhileIdle 과 함께 씁니다.
         ※ Google Play 배포 시 USE_EXACT_ALARM 은 알람/캘린더가 핵심 기능인 앱만 허용됩니다.
            사이드로드(직접 설치)에는 제한이 없습니다. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- 재부팅 후에도 예약된 알림을 복원하기 위해 필요합니다. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 말하기 연습(녹음) 기능: WebView 의 getUserMedia 가 동작하려면
         매니페스트 선언과 런타임 권한이 모두 필요합니다. 선언이 없으면 녹음이 전부 실패합니다. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
</manifest>
